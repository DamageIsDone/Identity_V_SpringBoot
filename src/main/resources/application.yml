# 服务器端口（默认8080，可自行修改）
server:
  port: 8080

# 微信小程序配置
wx:
  appid: "wx2fbf93c3e8401146"           # 从微信公众平台获取
  secret: "aed8c75e6e0ad8766a85a3bce53929a2"         # 从微信公众平台获取
  jscode2session-url: "https://api.weixin.qq.com/sns/jscode2session"

# 数据库配置（根据你已有的配置保留，这里只示例）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db?useSSL=false&serverTimezone=UTC
    username: root
    password: wstc70!QWQ
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    org.springframework.jdbc.core: TRACE
    org.springframework.jdbc.core.JdbcTemplate: TRACE
    org.springframework.jdbc.core.StatementCreatorUtils: TRACE